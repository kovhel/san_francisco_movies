import React from 'react';

import FilmsMapContainer from '../modules/map/containers/filmsMapContainer';

const Home = () => (
  <div style={{ height: '100%', overflow: 'hidden' }}>
    <FilmsMapContainer />
  </div>
);

export default Home;
